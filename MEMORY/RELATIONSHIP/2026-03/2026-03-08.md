# Relationship Notes: 2026-03-08

*Auto-captured from sessions. Manual additions welcome.*

---

## 18:17 PST

- B @ATOM: Tomorrow at 07:00 UTC it runs for the first time with the new stack

## 18:26 PST

- B @ATOM: - Added `export const runtime = 'edge'` to `app/blog/page.tsx` after the existing `force-dynamic` export
- B @ATOM: - Committed both blog route fixes (`app/blog/page.tsx`, `app/blog/[slug]/page.tsx`)
- B @ATOM: All 3 changes applied successfully

## 18:34 PST

- B @ATOM: - Found `app/[category]/[league]/page.tsx` in `/home/herbert/Projects/data-setpiecetakers/`
- B @ATOM: - Pushed `main` to `github.com:Derkaegi/data-setpiecetakers` (832caa8..7baa01d)
- B @ATOM: - **Root cause:** Next.js forbids `runtime = 'edge'` + `generateStaticParams` on the same route

## 19:14 PST

- B @ATOM: **1. Team Share % — Removed** (`components/data/DataTable.tsx`)

## 22:24 PST

- B @ATOM: Fix is live. Remember to run the Supabase cleanup query if you want to purge the "Global" row from the DB entirely — otherwise the display filter handles it silently.
- B @ATOM: Looks like Firecrawl CLI just initialized successfully. This gives your AI agents web scraping capabilities — scrape pages to markdown, search+scrape, crawl sites, map domains.
- B @ATOM: com/danielmiessler/TheAlgorithm) ═════════════

🗒️ TASK: Acknowledge Firecrawl CLI initialization output

📋 SUMMARY: Looks like Firecrawl CLI just initialized successfully

## 22:24 PST

- B @ATOM: All 5 penalty leagues + 6 corners + 6 free kicks leagues now write successfully
- B @ATOM: - Single root cause: expired `GOOGLE_SHEETS_REFRESH_TOKEN` in `~/.env`
